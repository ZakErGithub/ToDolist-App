<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List</title>
    <link rel="stylesheet" href="style.css" />
    <link rel="stylesheet" href="nav.css" />
  </head>
  <body>
    <div class="container">
      <h1>To Do List</h1>
      <ul id="todo-list">
        <% for (let i = 0; i < todos.length; i++) { %>
        <li>
          <div class="todo-item">
            <span class="todo-title"><%=todos[i].title%></span>
            <h3><%= todos[i].id %></h3>
            <p><%= todos[i].description %></p>
            <li><%= todos[i].completed %></li>
            <div class="buttons">
              <button class="update">
                <a href="/update/<%= todos[i].id %>">update</a>
              </button>
              <button class="delete">
                <a href="/delete/<%= todos[i].id %>">delete</a>
              </button>
          </div>
        </li>
        <% } %>
      </ul>
      <button class="add">
        <a href="/add">Add New Item</a>
        </button>
    </div>
  </body>
</html>
